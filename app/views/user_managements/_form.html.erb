<%= form_for(@user_management) do |f| %>
  <% if @user_management.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_management.errors.count, "error") %> prohibited this user_management from being saved:</h2>

      <ul>
      <% @user_management.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
