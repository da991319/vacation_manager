= form_for(@user_management) do |f|
  - if @user_management.errors.any?
    #error_explanation
      %h2
        = pluralize(@user_management.errors.count, "error")
        prohibited this user_management from being saved:
      %ul
        - @user_management.errors.full_messages.each do |msg|
          %li= msg
  .actions
    = f.submit
